import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TugasIndividu_34659',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
